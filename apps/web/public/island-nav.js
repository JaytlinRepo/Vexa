/* island-nav.js — intentionally empty.
 * The topbar island morph for dashboard views is handled entirely by
 * topbar-scroll.js (computeTargetP returns 1 for view-db-* views,
 * bindScroll animates the crossing from p=0→1 on login/navigate).
 * This file is kept so the <Script> tag in PrototypeShell doesn't 404.
 */
