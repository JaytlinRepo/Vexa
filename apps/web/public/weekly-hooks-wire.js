/* Weekly Hooks — RETIRED.
 *
 * Alex (copywriter) was removed from the team, so the weekly-hooks brief
 * card is no longer rendered. The export is kept as a no-op so any
 * legacy caller that still references it doesn't crash.
 */
window.renderWeeklyHooks = function () { return '' }
